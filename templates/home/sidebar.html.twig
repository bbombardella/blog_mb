<div class="d-flex flex-column flex-shrink-0 p-3 text-white bg-dark position-fixed vh-100">
	<ul class="nav nav-pills flex-column mb-auto">
		<li class="nav-item">
			<h5 class="mb-3">
				Catégories
			</h5>
			<div>
				<ul>
					{% for category in categories %}
						{% if category.posts|length > 0 %}
							<li>
								{{category.name}}
								-
								<small>{{ category.posts|length }}
									{% if category.posts|length == 1 %}
										post
									{% else %}
										posts
									{% endif %}
								</small>
							</li>
						{% endif %}
					{% else %}
						<p>Désolé, il n'y a aucunes catégories de disponibles.</p>
					{% endfor %}
				</ul>
			</div>
		</li>
		<li class="nav-item">
			<h5 class="mt-2 mb-3">
				Les derniers commentaires
			</h5>
			<ul class="px-2">
				{% for comment in comments %}
					<li class="my-2 mx-0 list-unstyled">
						<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-chat-left-text" viewbox="0 0 16 16">
							<path d="M14 1a1 1 0 0 1 1 1v8a1 1 0 0 1-1 1H4.414A2 2 0 0 0 3 11.586l-2 2V2a1 1 0 0 1 1-1h12zM2 0a2 2 0 0 0-2 2v12.793a.5.5 0 0 0 .854.353l2.853-2.853A1 1 0 0 1 4.414 12H14a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2z"/>
							<path d="M3 3.5a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9a.5.5 0 0 1-.5-.5zM3 6a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9A.5.5 0 0 1 3 6zm0 2.5a.5.5 0 0 1 .5-.5h5a.5.5 0 0 1 0 1h-5a.5.5 0 0 1-.5-.5z"/>
						</svg>
						<strong>{{comment.username}}</strong>
						a écrit :
						{{comment.content}}
					</li>
				{% else %}
					<p>Désolé, il n'y a aucun post de disponibles.</p>
				{% endfor %}
			</ul>
		</li>
	</ul>
</div>
