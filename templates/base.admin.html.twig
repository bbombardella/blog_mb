<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Accueil{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            {#Création du header#}
            <ul id="navigation">
                <li><a href="{{ item.href }}">{{ Nom du blog }}</a></li>
                <li><a href="{{ item.href }}">{{ Gérer les posts }}</a></li>
                <li><a href="{{ item.href }}">{{ Gérer les catégories }}</a></li>
                <li><a href="{{ item.href }}">{{ Gérer les commentaires }}</a></li>
            </ul>
        {% endblock %}
    </body>
</html>
